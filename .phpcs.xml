<?xml version="1.0"?>
<ruleset xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" name="Homepages" xsi:noNamespaceSchemaLocation="https://raw.githubusercontent.com/squizlabs/PHP_CodeSniffer/master/phpcs.xsd">
  <description>PHP_CodeSniffer standard for Homepages plugin.</description>

  <!-- Inherit from existing rulesets -->
  <rule ref="./vendor/alleyinteractive/alley-coding-standards/Alley-Interactive" />

  <!-- Allow short array syntax -->
  <rule ref="Universal.Arrays.DisallowShortArraySyntax.Found">
    <severity>0</severity>
  </rule>

  <!--
  Pass some flags to PHPCS:
    p flag: Show progress of the run.
    s flag: Show sniff codes in all reports.
  -->
  <arg value="ps" />

  <!-- Whenever possible, cache the scan results and re-use those for unchanged files on the next scan. -->
  <arg name="cache" value=".phpcs/cache.json" />

  <!-- Strip the filepaths down to the relevant bit. -->
  <arg name="basepath" value="./" />

  <!-- Check up to 20 files simultaneously. -->
  <arg name="parallel" value="20" />

  <!-- Set severity to 1 to see everything that isn't effectively turned off. -->
  <arg name="severity" value="1" />

  <!-- Exclude a few directories and autogenerated files. -->
  <exclude-pattern>build/</exclude-pattern>
  <exclude-pattern>vendor/</exclude-pattern>
  <exclude-pattern>tests/*</exclude-pattern>

  <!-- The version set here matches the minimum version tested in tests. -->
  <config name="minimum_supported_wp_version" value="6.0" />

	<rule ref="Alley-Interactive">
		<exclude name="WordPress.NamingConventions.ValidPostTypeSlug.NotStringLiteral" />
		<exclude name="WordPress.NamingConventions.PrefixAllGlobals.ShortPrefixPassed" />
	</rule>


  <!-- Text domain for the plugin -->
  <config name="text_domain" value="homepages" />

  <!-- Define the prefixes that can be used by the plugin -->
  <rule ref="WordPress.NamingConventions.PrefixAllGlobals">
    <properties>
      <property name="prefixes" type="array">
        <element value="homepages" />
      </property>
    </properties>
  </rule>

</ruleset>